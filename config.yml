title: Azure Kubernetes Services
description: Take this couse to learn Azure Kubernetes and get trained.
template:
  repo: azure-kubernetes-services-template
  name: azure-kubernetes-services
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
